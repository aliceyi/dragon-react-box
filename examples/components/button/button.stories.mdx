import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { Button } from '../../../packages/button/src/index'

<Meta title="MDX/Button" component={Button} />

<!--- This is your Story template function, shown here in React -->

# Button

To trigger a operaion.

# Install

```
    yarn add @ff-dragon/react-button
```

# Import

```
    import { Button } from @ff-dragon/react-button
```

# Option

<ArgsTable of={Button} />

# Code Example

### Primary Button

<!-- why show the MDXCreateElement tag, this is a storybooks issue, you can refer https://github.com/storybookjs/storybook/issues/11542 -->

<Canvas>
    <Story
        name="Primary"
        args={{
            btnType: 'button',
            children: 'Primary Button',
            modifier: 'primary',
            onClick: () => {
                alert('Primary Button')
            },
        }}
    >
        {(args) => <Button {...args} />}
    </Story>
</Canvas>

### Default Button

<Canvas>
    <Story
        name="Default-large"
        args={{
            btnType: 'button',
            children: 'Default Large',
            modifier: 'default',
            size: 'large',
        }}
    >
        {(args) => <Button {...args} />}
    </Story>
    <Story
        name="Default-medium"
        args={{
            btnType: 'button',
            children: 'Default Medium',
            modifier: 'default',
            size: 'medium',
        }}
    >
        {(args) => <Button {...args} />}
    </Story>
    <Story
        name="Default-small"
        args={{
            btnType: 'button',
            children: 'Default Small',
            modifier: 'default',
            size: 'small',
        }}
    >
        {(args) => <Button {...args} />}
    </Story>
</Canvas>

### Default Icon Button

<Canvas>
    <Story
        name="Default-Icon-large"
        args={{
            btnType: 'button',
            children: 'Default Icon Large',
            modifier: 'default',
            size: 'large',
            icon: 'IconEdit',
        }}
    >
        {(args) => <Button {...args} />}
    </Story>
    <Story
        name="Default-Icon-medium"
        args={{
            btnType: 'button',
            children: 'Default Icon Medium',
            modifier: 'default',
            size: 'medium',
            icon: 'IconEdit',
        }}
    >
        {(args) => <Button {...args} />}
    </Story>
    <Story
        name="Default-Icon-small"
        args={{
            btnType: 'button',
            children: 'Default Icon Small',
            modifier: 'default',
            size: 'small',
            icon: 'IconEdit',
        }}
    >
        {(args) => <Button {...args} />}
    </Story>
</Canvas>

### Text Button

<Canvas>
    <Story
        name="Text-Icon-large"
        args={{
            btnType: 'text',
            children: 'Text Button Large',
            size: 'large',
            icon: 'IconEdit',
        }}
    >
        {(args) => <Button {...args} />}
    </Story>
    <Story
        name="Text-Icon-medium"
        args={{
            btnType: 'text',
            children: 'Text Button Medium',
            size: 'medium',
            icon: 'IconEdit',
        }}
    >
        {(args) => <Button {...args} />}
    </Story>
    <Story
        name="Text-Icon-small"
        args={{
            btnType: 'text',
            children: 'Text Button Small',
            size: 'small',
            icon: 'IconEdit',
        }}
    >
        {(args) => <Button {...args} />}
    </Story>
</Canvas>

### Icon Button

<Canvas>
    <Story
        name="Icon-Button-large"
        args={{
            btnType: 'icon',
            children: 'Large',
            size: 'large',
            icon: 'IconEdit',
        }}
    >
        {(args) => <Button {...args} />}
    </Story>
    <Story
        name="Icon-Button-medium"
        args={{
            btnType: 'icon',
            children: 'Medium',
            size: 'medium',
            icon: 'IconEdit',
        }}
    >
        {(args) => <Button {...args} />}
    </Story>
</Canvas>
